use crate::framework::graphics::GraphicsAdapter;
use crate::shared::XnaResult;

impl GraphicsAdapter {
    pub fn adapters() -> XnaResult<Vec<GraphicsAdapter>> {
        let mut adapters : Vec<GraphicsAdapter> = Vec::new();

        Ok(Vec::new())
    }
}

